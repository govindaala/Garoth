name: guideline_app
description: "Collector Guideline Rate Finder App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.0
  google_mobile_ads: 
  shared_preferences: ^2.3.2
  uuid: ^4.5.1

flutter:
  uses-material-design: true
  
